# CLAUDE.md

This project uses the **tlab** plugin. The canonical, cross-tool agent instructions live in
`AGENTS.md` (read natively by Codex and Antigravity). The line below imports that file so Claude Code
loads the same instructions:

@AGENTS.md
